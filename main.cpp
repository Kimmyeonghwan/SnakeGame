/**
 * @file main.cpp
 * @author 김명환(20181582), 임태민(20171690)
 * @brief 
 * @version 1.0
 * @date 2021-06-01
 */

#include <iostream>
#include <ncurses.h>
#include "UI.h"
#include "Map.h"
using namespace std;

int main(int argc, char *argv[])
{
	UI();
	/*
     Map fin;
     fin.readFile();

     Map m = fin.convertGameMap();

     auto mm = m.map;
     for (auto list : mm) {
         for (auto obj : list) {
             cout << obj;
         }
         cout << endl;
     }
	 */
	 
}
